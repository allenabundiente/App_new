# Release signing config for HulogTrack.
# Committed intentionally so GitHub Actions can sign every build with the same
# key (install updates over older versions). The key is only for this demo app —
# for a production app, generate a private keystore and pass it via CI secrets
# instead (see DEPLOY.md → Releasing a new APK).
storeFile=hulogtrack-release.keystore
storePassword=HulogTrack#2026
keyAlias=hulogtrack
keyPassword=HulogTrack#2026
